// Types for data/featureFlags.js — the E2E specs read the same
// constant the app does, so a switched-off feature takes its own
// coverage with it rather than leaving specs that assert nothing.
export const ELEMENTAL_NOTICES_ENABLED: boolean;
